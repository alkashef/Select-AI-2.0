# Logging settings
LOG_FILE=./logs/log.txt
LOG_ENABLED=true

# AI backend
AI_BACKEND=gpt

# OpenAI settings
GPT_MODEL=gpt-4o
OPENAI_TIMEOUT=20
OPENAI_API_KEY=
GPT_TEMPERATURE=0.7

# Google settings
GEMINI_MODEL=gemini-2.5-flash
GOOGLE_API_KEY=
GOOGLE_TIMEOUT=20

# Testing settings
# Only run real OpenAI tests when explicitly enabled (costs may apply).
# Leave blank by default so tests remain skipped.
RUN_REAL_OPENAI_TESTS=1

# TD connection (used by tests/test_db.py only; MCP server reads its own env)
TD_HOST=
TD_NAME=
TD_USER=
TD_PASSWORD=
TD_PORT=1025
# Optional convenience URI for direct DB scripts; do NOT include quotes
DATABASE_URI=

# MCP client settings (HTTP flow)
# URL of a separately started teradata-mcp-server in streamable-http mode
MCP_URL=http://localhost:8001/mcp/
MCP_TRANSPORT=stdio
LOG_LEVEL=DEBUG

# Langchain configuration
VERBOSE=True
MAX_ITERATIONS=30
RETURN_INTERMEDIATE_STEPS=True