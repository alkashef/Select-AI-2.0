"""Deprecated helper: RAG config is now managed via server defaults.

This script is no longer used by the project and will be removed in a future
cleanup. See `docs/teradata-mcp-server/LLM_MCP_INTEGRATION.md` for current
integration instructions.
"""

if __name__ == "__main__":
    print(
        "scripts/rag_config.py is deprecated and unused. "
        "See docs/teradata-mcp-server/LLM_MCP_INTEGRATION.md."
    )
